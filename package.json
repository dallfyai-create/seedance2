{
  "name": "seedance-studio-server",
  "version": "1.0.0",
  "description": "Backend proxy for Seedance Studio's Image Analyzer tab — keeps the Anthropic API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
